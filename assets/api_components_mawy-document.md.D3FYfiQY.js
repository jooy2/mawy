import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.Cd7mb1YL.js";const g=JSON.parse('{"title":"MawyDocument","description":"A document drawn once, on a server, with no JavaScript shipped for it.","frontmatter":{"title":"MawyDocument","order":3},"headers":[],"relativePath":"api/components/mawy-document.md","filePath":"en/api/components/mawy-document.md","lastUpdated":1789277504000}'),d={name:"api/components/mawy-document.md"};function i(r,e,n,h,c,l){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="mawydocument" tabindex="-1"><code>MawyDocument</code> <a class="header-anchor" href="#mawydocument" aria-label="Permalink to &quot;\`MawyDocument\`&quot;">​</a></h1><p>A document drawn once, on a server, with no JavaScript shipped for it.</p><div class="mawy-fw" data-fw="flutter"><p>This one is the React package&#39;s. A Flutter application draws its documents on the device, so there is no server half to it — <a href="./mawy-viewer"><code>MawyViewer</code></a> is the whole of what this package renders with.</p></div><div class="mawy-fw" data-fw="react"><p><code>MawyViewer</code> renders on a server perfectly well and then hydrates, because everything it offers a reader is behaviour: the toolbar, the find bar, the outline and the copy buttons all need the component on the page. A documentation site, a blog or a changelog needs none of that, and <code>mawy-react/server</code> exists so a page of documents does not ship forty kilobytes of JavaScript.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MawyDocument } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;mawy-react/server&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;mawy-react/styles.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Page</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyDocument</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> readFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;README.md&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;utf8&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)} /&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A React Server Component in a framework that has them, and an ordinary component to <code>renderToStaticMarkup</code> in one that does not. The markup is the same markup and the stylesheet is the same stylesheet, so a page built this way and a page with a viewer on it look alike.</p><p><a href="./../../guide/publishing">Publishing</a> is the whole of what a page needs around one of these when people are going to find it through a search engine.</p><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>value</code></td><td><code>string</code></td><td>—</td><td>The Markdown.</td></tr><tr><td><code>parse</code></td><td><a href="./../types/parse-options"><code>MawyParseOptions</code></a></td><td><code>{ gfm: true, breaks: false, definitionLists: true }</code></td><td>How it is read.</td></tr><tr><td><code>html</code></td><td><a href="./../types/html-policy"><code>MawyHtmlPolicy</code></a></td><td><code>&#39;escape&#39;</code></td><td>What becomes of raw HTML written inside it.</td></tr><tr><td><code>linkTarget</code></td><td><a href="./../types/link-target"><code>MawyLinkTarget</code></a></td><td><code>&#39;blank&#39;</code></td><td>Where a link the document wrote opens.</td></tr><tr><td><code>linkRel</code></td><td><a href="./../types/link-target#mawylinkrel"><code>MawyLinkRel</code></a></td><td>—</td><td>What such a link declares about where it goes. Added to what it already declares.</td></tr><tr><td><code>links</code></td><td><a href="./../types/drawing-policy#mawylinkpolicy"><code>MawyLinkPolicy</code></a></td><td><code>&#39;show&#39;</code></td><td>How a link the document wrote is drawn: as a link, as its words, as its source, or not at all.</td></tr><tr><td><code>images</code></td><td><a href="./../types/drawing-policy#mawyimagepolicy"><code>MawyImagePolicy</code></a></td><td><code>&#39;show&#39;</code></td><td>How a picture is drawn: fetched, as its description, as its source, or not at all.</td></tr><tr><td><code>directives</code></td><td><a href="./../types/directives"><code>MawyDirectives</code></a></td><td>—</td><td>What draws the constructs this package does not know about.</td></tr><tr><td><code>image</code></td><td><code>ComponentType&lt;</code><a href="./../types/image"><code>MawyImageProps</code></a><code>&gt;</code></td><td>—</td><td>What draws a picture the document points at, and how a framework&#39;s own image component gets used.</td></tr><tr><td><code>resolveUrl</code></td><td><a href="./../types/url-resolver"><code>MawyUrlResolver</code></a></td><td>—</td><td>Where a relative URL in the document points.</td></tr><tr><td><code>headingBase</code></td><td><code>number</code></td><td><code>1</code></td><td>Which of <code>h1</code> to <code>h6</code> the document&#39;s own <code>#</code> is drawn as.</td></tr><tr><td><code>anchorPrefix</code></td><td><code>string</code></td><td>—</td><td>Put in front of every anchor this drawing gives a heading or a footnote, so two documents on one page stop colliding.</td></tr><tr><td><code>locale</code></td><td><a href="./../types/locale"><code>MawyLocale</code></a></td><td><code>&#39;en&#39;</code></td><td>The language of the few words this library writes itself.</td></tr><tr><td><code>strings</code></td><td><code>Partial&lt;</code><a href="./../types/locale#mawystrings"><code>MawyStrings</code></a><code>&gt;</code></td><td>—</td><td>The interface&#39;s words, some or all of them, over the ones <code>locale</code> has.</td></tr><tr><td><code>highlight</code></td><td><a href="./../types/highlighter"><code>MawyHighlighter</code></a></td><td>—</td><td>What colours a code block.</td></tr><tr><td><code>typography</code></td><td><code>Partial&lt;</code><a href="./../types/typography"><code>MawyTypography</code></a><code>&gt;</code></td><td>—</td><td>How the document is set, as the same custom properties. Anything left out keeps its default.</td></tr><tr><td><code>fonts</code></td><td><a href="./../types/font"><code>MawyFont</code></a><code>[]</code></td><td><code>MAWY_SYSTEM_FONTS</code></td><td>The typefaces those properties may name.</td></tr><tr><td><code>colorScheme</code></td><td><a href="./../types/color-scheme"><code>MawyColorScheme</code></a><code> | null</code></td><td><code>null</code></td><td>Which palette to draw in. <code>null</code> writes nothing and leaves it to the page.</td></tr><tr><td><code>className</code></td><td><code>string</code></td><td>—</td><td>Put on the outermost element, after this library&#39;s own names.</td></tr><tr><td><code>style</code></td><td><code>CSSProperties</code></td><td>—</td><td>Merged over the custom properties the typography writes.</td></tr></tbody></table><h2 id="following-the-reader-s-palette" tabindex="-1">Following the reader&#39;s palette <a class="header-anchor" href="#following-the-reader-s-palette" aria-label="Permalink to &quot;Following the reader&#39;s palette&quot;">​</a></h2><p><code>colorScheme</code> is <code>null</code> by default, which writes no attribute at all. That is what an application setting the <code>--mawy-*</code> tokens itself wants: a palette declared here would be one more thing for its own to argue with.</p><p>It also means the document is light on a dark screen, because the stylesheet turns a document dark under <code>prefers-color-scheme</code> only where the attribute says <code>system</code>. A page with no palette of its own says so:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyDocument</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{post.body} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">colorScheme</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;system&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div><p>Which is what <a href="./mawy-viewer"><code>MawyViewer</code></a> does by default. The difference in default is deliberate — a viewer is a surface a reader is looking at, and this is a piece of somebody&#39;s page.</p><h2 id="moving-a-page-from-mawyviewer" tabindex="-1">Moving a page from <code>MawyViewer</code> <a class="header-anchor" href="#moving-a-page-from-mawyviewer" aria-label="Permalink to &quot;Moving a page from \`MawyViewer\`&quot;">​</a></h2><p>Everything either of them takes about <em>how</em> a document is drawn is the same prop with the same type, so moving a page is deleting the props that were behaviour — <code>toolbar</code>, <code>empty</code>, <code>fileDrop</code>, <code>accept</code>, and the controlled and uncontrolled pairs — and rewriting none of the rest.</p><p>Two are deliberately not shared, and the compiler names both:</p><ul><li><strong><code>value</code> is required here.</strong> A viewer with no document is the file picker. A document with no document is nothing.</li><li><strong><code>highlight</code> takes only a highlighter</strong>, where the viewer also takes a function that fetches one. A promise has no second render to arrive on, so accepting one and ignoring it would be worse than refusing it.</li></ul><h2 id="what-is-not-there-and-why" tabindex="-1">What is not there, and why <a class="header-anchor" href="#what-is-not-there-and-why" aria-label="Permalink to &quot;What is not there, and why&quot;">​</a></h2><ul><li><strong>No toolbar, find bar or outline.</strong> Each needs JavaScript to work, and there is none here. Use <a href="./mawy-viewer"><code>MawyViewer</code></a> if you need them.</li><li><strong>No copy button on a code block</strong>, for the same reason.</li><li><strong><code>html=&quot;sanitize&quot;</code> draws most markup as characters.</strong> Sanitising needs a DOM to parse with, and a server has none. <code>MawyViewer</code> does the same on a server, except that there the elements arrive on the next render. Here there is no next render. The exceptions are the few pieces a browser has only one way to read, so no parser is needed for them: a <code>&lt;br&gt;</code>, an <code>&lt;img&gt;</code> whose attributes are nothing but quoted <code>src</code>, <code>alt</code>, <code>width</code>, <code>height</code> and <code>title</code>, and a tag like <code>&lt;u&gt;</code> around some words. See <a href="./../../guide/viewer#safety">safety</a>. <code>html=&quot;raw&quot;</code> writes the markup out as the author wrote it, with everything <a href="./../../guide/viewer#safety">that means</a>.</li><li><strong>A highlighter is used only if it answers synchronously.</strong> A promise has no second render to arrive on. Pass <code>mawyHighlighter</code>, or any other synchronous highlighter, and the colour is in the HTML.</li><li><strong>No <code>data-mawy-range</code> on anything.</strong> Every element <code>MawyViewer</code> draws carries the offsets it was drawn from, so that a place on the page can be turned back into a place in the document — see <a href="./../../guide/viewer#mapping-the-page-back-to-the-source">mapping the page back to the source</a>. Nothing on a page built this way asks that question, and the attribute is a quarter of the HTML: this repository&#39;s own README comes out at 12.2 kB rather than 16.8 kB, and 3.4 kB rather than 4.6 kB gzipped.</li></ul></div>`,4)])])}const y=t(d,[["render",i]]);export{g as __pageData,y as default};
