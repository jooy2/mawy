import{_ as i,C as o,o as n,c as d,a2 as t,E as s}from"./chunks/framework.Cd7mb1YL.js";const w=JSON.parse(`{"title":"Editor","description":"This package has three surfaces, and the React package's wysiwyg is the one it does not have. wysiwyg draws the document and edits it where it is drawn, which…","frontmatter":{"title":"Editor","order":2},"headers":[],"relativePath":"guide/editor.md","filePath":"en/guide/editor.md","lastUpdated":1788614595000}`),h={name:"guide/editor.md"};function r(c,e,l,p,g,k){const a=o("MawyDemo");return n(),d("div",null,[e[0]||(e[0]=t(`<h1 id="the-editor" tabindex="-1">The editor <a class="header-anchor" href="#the-editor" aria-label="Permalink to &quot;The editor&quot;">​</a></h1><div class="mawy-fw" data-fw="flutter"><p><strong>This package has three surfaces</strong>, and the React package&#39;s <code>wysiwyg</code> is the one it does not have. <code>wysiwyg</code> draws the document and edits it where it is drawn, which rests entirely on <code>contenteditable</code>: the browser tells the component what somebody tried to do to the tree, and the component refuses it and changes the Markdown instead. Flutter has no equivalent, because an <code>EditableText</code> owns its string. Drawing a document that is also a text field would need a second model of the document, and two models read anything unusual differently.</p><p>So the surfaces are <code>plain</code>, <code>split</code> and <code>preview</code>, and the drawn surface stays a viewer. Nearly everything else on this page applies here as well. The commands, the colouring of the source, the counts along the bottom and finding text are the same functions under the same names, and the parity check diffs all four against the React package&#39;s on every change. Opening and saving are the exception, and that section says why.</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;# Hello&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  onChange</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> save,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div></div><p>Mawy&#39;s editor is one component with several surfaces. <code>plain</code> edits the Markdown source as text, <code>preview</code> shows the rendered document, and <code>split</code> shows both at once. All three show the same value, which is where the design starts.</p>`,3)),s(a,{name:"editor/basic",flutter:"editor/basic",height:520}),e[1]||(e[1]=t(`<div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MawyEditor } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;mawy-react&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Page</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;# Hello&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> onChange</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{save} /&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="several-surfaces-one-value" tabindex="-1">Several surfaces, one value <a class="header-anchor" href="#several-surfaces-one-value" aria-label="Permalink to &quot;Several surfaces, one value&quot;">​</a></h2><p>The usual arrangement is a WYSIWYG editor with a &quot;source mode&quot; bolted on, where switching serialises out of one model and parses into another. Every round trip through that pair loses whatever the other side could not express: a footnote, an HTML block, the exact spelling of a list marker. Nothing reports the loss.</p><p>Here the document is a string of Markdown and every surface is a way of looking at it. Switching changes which surface is mounted; it does not re-serialise anything, so a document that came in unchanged goes out unchanged.</p><h2 id="modes" tabindex="-1">Modes <a class="header-anchor" href="#modes" aria-label="Permalink to &quot;Modes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Mode</th><th>Editable</th><th>Shows</th></tr></thead><tbody><tr><td><code>&#39;plain&#39;</code></td><td>Yes</td><td>The Markdown source</td></tr><tr><td><code>&#39;preview&#39;</code></td><td>No</td><td>The rendered document</td></tr><tr><td><code>&#39;split&#39;</code></td><td>Yes</td><td>Both, side by side</td></tr><tr><td><code>&#39;wysiwyg&#39;</code></td><td>In part</td><td>The rendered document, in place</td></tr></tbody></table><p><code>modes</code> decides which of them the toolbar offers, and the switch disappears when there is only one:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">modes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;plain&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]} /&gt;</span></span></code></pre></div>`,8)),s(a,{name:"editor/source"}),e[2]||(e[2]=t('<p><code>mode</code> / <code>defaultMode</code> / <code>onModeChange</code> are the usual three: pass <code>mode</code> and the application decides, pass <code>defaultMode</code> and the editor keeps it, and <code>onModeChange</code> is called either way.</p><p>In <code>split</code>, the preview scrolls with the source <strong>to the block</strong> rather than to the same fraction of the way down the file. Whichever line is at the top of the source decides which block is at the top of the preview, and the positions in between run straight from one block to the next. A fraction cannot do that. A code block takes the same number of lines in both panes, but the prose around it does not, so the two drift apart as soon as they are mixed.</p><p><strong>The bar between the two panes can be dragged.</strong> An even split is not always the right one: a wide window wants more preview while you read over a draft and more source while you write one. Drag it, or focus it and use the arrows. <code>Shift</code> takes a bigger step, <code>Home</code> and <code>End</code> go to the ends, and <code>Enter</code> or a double-click restores the even split. It stops well short of either edge, because a pane pushed to nothing cannot be dragged back.</p><p>The bar&#39;s position lasts as long as the reader is on the page, and there is no prop for it. A pane&#39;s edge is the same kind of value as a scroll position. The value worth storing is the document, and <code>value</code> and <code>onChange</code> handle that.</p><p>Below the width at which the two panes stack instead of sitting side by side, the bar is not drawn.</p><p><strong>The preview is read-only, and nothing is edited from it.</strong> A click there does not move the caret in the source, and that is the difference between this pane and <code>wysiwyg</code>, which is the one surface that edits the drawn document. If a click in the preview moved the caret, it would be unclear which pane you are writing in. Links, checkboxes and a code block&#39;s copy button still work, and everything else can be selected and copied like any other page.</p><h2 id="the-source-surface" tabindex="-1">The source surface <a class="header-anchor" href="#the-source-surface" aria-label="Permalink to &quot;The source surface&quot;">​</a></h2><div class="mawy-fw" data-fw="react"><p>A real <code>&lt;textarea&gt;</code>, with a coloured copy of the same text laid exactly underneath it.</p><p>That arrangement is deliberate. The textarea keeps everything that is hard to reimplement and obvious when it is missing: the <strong>IME</strong> (Korean is composed a jamo at a time, and an editor that intercepts the composition loses characters), the mobile keyboard, autocorrect, spellcheck, and every text-selection gesture the platform has. What a textarea cannot do is colour anything, so its text is made transparent, its caret and selection are left visible, and a layer behind it draws the same characters in colour.</p><p>Everything that decides where a character lands is declared once in the stylesheet, for both layers: font, size, line height, letter spacing, tab size, wrapping, and the padding that sets the left edge. That declaration keeps the two layers in step, and it is why the editor reads its monospace face from <code>--mawy-font-mono</code> instead of choosing one.</p><p>A document past about six hundred lines is not drawn a row per line. The copy underneath is cut into chunks, and only the chunks near the view are rows with the syntax coloured in. The rest hold the same characters as one run of text, at the same height and a fraction of the elements. Nothing is left out, so the browser&#39;s own find still reaches the whole document, printing still puts all of it on the paper, and the copy is still exactly as tall as the field. A scroll fast enough to outrun the colouring shows plain text in the right place for a frame, never a gap.</p></div><div class="mawy-fw" data-fw="flutter"><p>One <code>EditableText</code>, coloured in place.</p><p>This is the one place this package has the easier job. A browser gives no way to colour what is inside a text field, so the React package draws two layers and keeps them in step. A <code>TextEditingController</code> takes the spans to draw directly, and <code>MawySourceController</code> supplies them. One layer, no grid to keep aligned, and the caret and the selection behave as the platform&#39;s own because the field is the platform&#39;s own.</p><p>What that field does not come with is pointer handling. A bare <code>EditableText</code> puts the caret where it is tapped and stops there: dragging across it selects nothing, a double tap takes no word, and a long press raises no handles. All of that is <code>TextSelectionGestureDetectorBuilder</code>, which <code>TextField</code> builds around its own field and this widget builds around its own. It lives in <code>package:flutter/widgets.dart</code> rather than in Material, so using it does not pull Material in.</p><p>Two things come from the platform rather than this package, and both live in Cupertino rather than Flutter: the magnifier that opens under a hard press on iOS, and the selection toolbar. Rather than start a gesture this package cannot finish, the force press is off.</p><p><code>lineNumbers</code> turns the gutter off here too. It is on by default, and the numbers are painted from the field&#39;s own laid-out lines rather than from a second layout of the same text. A line that wrapped is two rows on the screen and one number down the side, and with only one layout the two cannot drift.</p><p>A document past about six hundred lines is coloured where it can be seen and handed over as plain characters everywhere else. The field lays the whole document out either way. A single layer cannot avoid that, and it is also what keeps the caret, the selection and every measurement exactly what they were. What is saved is reading every line on every keystroke. The window is worked out after a frame rather than during one, so a scroll fast enough to outrun it shows plain text in the right place until the next frame.</p></div><p>The syntax colouring uses the token names of Mawy&#39;s parser, but not the parser itself. A line being typed is half-written most of the time, and a highlighter that waited for <code>**bold</code> to close before reacting would flicker on every keystroke. So it reads a line at a time, approximately, and leaves an unfinished marker alone.</p><div class="mawy-fw" data-fw="react"><p><code>lineNumbers</code> turns the gutter off. It is on by default, and the numbers stay lined up with soft-wrapped text because both layers share one grid.</p></div><h2 id="the-document-surface" tabindex="-1">The document surface <a class="header-anchor" href="#the-document-surface" aria-label="Permalink to &quot;The document surface&quot;">​</a></h2><p><code>wysiwyg</code> draws the document and lets you edit it where it is drawn. Its behaviour is easy to assume, so the rest of this section spells it out.</p>',13)),s(a,{name:"editor/document"}),e[3]||(e[3]=t(`<p>There is no second model behind it. What is on screen is a drawing of the Markdown, and the Markdown string is the source of truth. Every keystroke is refused, turned into an edit to that string, and the document is drawn again from whatever the string became. Nothing is ever read back out of the tree the browser wanted to change. There is no DOM-to-Markdown serialiser in this package and there is not going to be one, because two implementations read anything unusual differently.</p><p><strong>It is the first surface the switch offers.</strong> A link&#39;s destination and drawn raw HTML are both written out as their own characters when the caret is in them, so there is nowhere on this surface a caret cannot go. Until those two worked it was not on the default list. To leave it out, drop it from <code>modes</code>:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">modes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;plain&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;split&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;preview&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]} /&gt;</span></span></code></pre></div><p>What works: typing and deleting <strong>anywhere there is text to type in</strong>, meaning a paragraph, a heading, a list item, a quotation, a table cell or a code block. So do replacing a selection, <code>Shift</code>+<code>Enter</code> for a hard break, the shorthands that turn into formatting as they are typed, and every command on the toolbar. The toolbar commands needed no new code, because they are pure functions of the source and its selection and name no surface at all.</p><p>Edits land on the <strong>drawn</strong> character rather than the written one, which is the core of this surface. The caret after <code>bold</code> in <code>**bold**</code> has an asterisk in front of it in the file and a <code>d</code> in front of it on the page. <code>Backspace</code> there takes the <code>d</code>. An image and a hard break come out in one piece, because each is one character to a reader and invisible to a walk over the runs of text.</p><p>A caret in a place the page cannot draw is the same problem. Markdown does not keep the whitespace at the end of a line, so a space typed at the end of a paragraph is in the file and drawn nowhere at all, and the caret can only come back in front of it. Where it was <strong>meant</strong> to be is kept beside the place it settled for, so the next letter goes after the space rather than in front of it. Without that, <code>One two</code> could not be typed a word at a time. <code>Backspace</code> there takes the space, because there is no drawn character in front of the caret to take.</p><p><code>Enter</code> is a different thing in every container it is pressed in, because a blank line means something different in each:</p><table tabindex="0"><thead><tr><th>Where</th><th>What <code>Enter</code> does</th></tr></thead><tbody><tr><td>Between blocks</td><td>A blank line</td></tr><tr><td>In a list item</td><td>A new item, marker carried down. On an item still empty, the marker goes</td></tr><tr><td>In a quotation</td><td>Ends the paragraph. Continuing it takes a <em>blank quoted line</em></td></tr><tr><td>In a code block</td><td>A newline and nothing else</td></tr><tr><td>In a table</td><td>Nothing: a row is a line, and there is nowhere in the file for a second one</td></tr></tbody></table><p><code>Backspace</code> at the start of a block joins it to the one before it: two list items run together, a paragraph joins the heading above it. Two joins are refused. Joining a table cell to the cell beside it would remove the pipe between them, and joining a code block to whatever is above it would remove the fence.</p><p><code>Enter</code> at the end of a paragraph is the one place the file cannot express what the screen needs. Markdown has no empty paragraph: a blank line separates two blocks and a second blank line separates the same two. So the surface draws one anyway, in that one place, for as long as the caret is in it. The moment anything is typed, the paragraph is a real one.</p><p>An input method is the one thing that <strong>cannot</strong> be refused, so it is handled the other way round. A composition is left completely alone: from <code>compositionstart</code> to <code>compositionend</code> the browser owns that run of text, nothing is prevented, and nothing is redrawn in between. When it finishes, the run is compared with what it said before and the difference goes into the Markdown at the place that run came from.</p><p>Refusing a composition prevents the composition itself. Korean is composed a jamo at a time, and an editor that blocks each one cannot write Korean at all. Pressing <code>Enter</code> and composing straight into the empty paragraph works too, because with no run of text to be in yet, the block itself is remembered.</p><p><strong>A link, an image or a piece of raw HTML the caret is inside is written out as its own characters</strong>, destination and all, and drawn back as itself when the caret leaves. This happens only while the editor has the focus, so a document that opens with a link does not show its brackets to somebody who has not touched it. A drawn <code>&lt;a&gt;</code> puts its words on the page and never its <code>(url)</code>, so before this there was nowhere on the page to edit a destination, and the toolbar&#39;s <code>[](url)</code> arrived as a placeholder nobody could type over. Written out, it is the source one character for one, and every rule this surface already has works on it unchanged. What is under the caret is shown rather than hidden, because text that has stopped being a link and become the text that makes one is worth seeing.</p><p><strong>A block written so far as its marker and nothing else is written out the same way.</strong> <code>#</code> on its own is an empty heading to CommonMark, and the parser reads it that way, but an empty heading draws no characters at all. The <code>#</code> somebody typed disappeared as they typed it, and somebody who wanted a <code>#</code> in a sentence had a heading to undo. Written out, the <code>#</code> stays on screen, and the space that would make it a heading is one keystroke away. <code>-</code>, <code>&gt;</code>, <code>1.</code> and <code>- [ ]</code> are the same, with a bullet, a bar, a number or a box left where the text went. The outermost such block is the one written out, so a list holding one empty item writes out the list rather than the item. Drawing a bullet beside the <code>-</code> that stands for it is no improvement.</p><p><strong>Raw HTML drawn as elements rather than shown as characters is written out the same way</strong>, under <code>sanitize</code> and <code>raw</code> alike. Markup that reached the page through <code>dangerouslySetInnerHTML</code> is markup React does not know the inside of and could not put back, so the drawn form has nowhere for a caret to be. Written out, it does. Under the default <code>escape</code>, it was always plain text and is edited like any other text.</p><h2 id="input-rules" tabindex="-1">Input rules <a class="header-anchor" href="#input-rules" aria-label="Permalink to &quot;Input rules&quot;">​</a></h2><p>On the drawn document the shorthand you type becomes the formatting it is shorthand for, where you typed it. <code># </code> is a heading, <code>- </code> and <code>* </code> a bullet, <code>1. </code> a number, <code>&gt; </code> a quotation, <code>- [ ] </code> a task box.</p><p><strong>Most of that is not code we wrote.</strong> The document is drawn again from the Markdown after every keystroke, so <code># </code> at the start of a paragraph <em>is</em> a heading the moment the space lands. The parser reads it that way and the drawing follows. There is nothing to configure and nothing to turn off.</p><p>The moment the space lands, and not before. <code>#</code> on its own is drawn as the character it is until there is something in the heading it would make. That is <a href="#the-document-surface">the document surface</a> writing out a block with nothing in it, not an input rule, and it is why a <code>#</code> in a sentence can be typed at all.</p><p>Two rules are written down, and they are the two where a marker changes the meaning of text nobody is typing:</p><ul><li><strong>Three backticks open a fence, and a fence runs until one closes it.</strong> Typed halfway down a document they would put everything under them inside a code block and leave it there until the closing fence was typed. So a fence is opened <em>closed</em>: the caret goes between the two and whatever was on the line goes inside, which is the same thing the code-block button does. Inside a list item or a quotation the two lines it adds carry that container&#39;s own prefix, or the fence would close outside the item it opened in.</li><li><strong>A thematic break has no text in it.</strong> <code>---</code> on its own line is a break the moment the third dash lands, and the caret is then in a block that draws no characters at all, with nowhere on the page to be. So a break is given a blank line under it to carry on typing on. <code>***</code>, <code>___</code> and <code>- - -</code> are the same. <code>---</code> under a line that is still going is left alone: there it is that paragraph&#39;s underline, a setext heading, and the parser reads it correctly.</li></ul><p>Inside a code block none of it happens, because everything in there is the characters it is.</p><p>A rule that writes a line ending is its own step on the undo stack, so <code>Mod</code>+<code>Z</code> straight after one gives back the characters that set it off rather than the whole run of typing around it.</p><p>The source surface has no input rules. The characters typed are the document, and <code># </code> at the start of a line is already a heading.</p><h2 id="formatting" tabindex="-1">Formatting <a class="header-anchor" href="#formatting" aria-label="Permalink to &quot;Formatting&quot;">​</a></h2><p>Every button on the toolbar runs a command that also has a keyboard shortcut. No command is reachable only from the toolbar, because that would make the editor unusable without a pointer.</p><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><code>Mod</code> + <code>B</code></td><td>Bold</td></tr><tr><td><code>Mod</code> + <code>I</code></td><td>Italic</td></tr><tr><td><code>Mod</code> + <code>Shift</code> + <code>X</code></td><td>Strikethrough</td></tr><tr><td><code>Mod</code> + <code>E</code></td><td>Code</td></tr><tr><td><code>Mod</code> + <code>K</code></td><td>Link</td></tr><tr><td><code>Mod</code> + <code>1</code> / <code>2</code> / <code>3</code></td><td>Heading 1, 2, 3</td></tr><tr><td><code>Mod</code> + <code>0</code></td><td>Body text</td></tr><tr><td><code>Mod</code> + <code>F</code></td><td>Find and replace</td></tr><tr><td><code>Mod</code> + <code>S</code></td><td>Save</td></tr><tr><td><code>Tab</code> / <code>Shift</code> + <code>Tab</code></td><td>Indent, outdent</td></tr><tr><td><code>Mod</code> + <code>Z</code></td><td>Undo</td></tr><tr><td><code>Mod</code> + <code>Shift</code> + <code>Z</code></td><td>Redo, and <code>Ctrl</code> + <code>Y</code> as well</td></tr><tr><td><code>Enter</code></td><td>Carries a list marker to the next line</td></tr></tbody></table><p><code>Mod</code> is Command or Control, whichever the machine has. Both are accepted rather than guessed at.</p><div class="mawy-fw" data-fw="flutter"><p><code>Mod</code>+<code>S</code> is the one line of that table this package does not handle, because there is nothing here to save to. <a href="#opening-and-saving">Opening and saving</a> has the reason. Undo belongs to the platform rather than this package, as <a href="#undo">undo</a> explains, and everything else is the same keyboard.</p></div><p>Every command is a <strong>toggle</strong>: pressing <code>Mod</code>+<code>B</code> on bold text unbolds it, and the toolbar button shows which are in force. Markers replace each other rather than stacking, so turning a numbered list into a bulleted one gives a bulleted list and not <code>- 1. item</code>.</p><p><code>Enter</code> at the end of a list item carries the marker down and counts an ordered list on. Pressing it again on the item that is still empty takes the marker away instead of making another. Without that, leaving a list would mean deleting the bullet the editor had just added.</p><p>The edits go in through the browser&#39;s own text-insertion command, which leaves the caret, the scroll position and any composition in progress exactly where they were. Writing the value directly guarantees none of that. Undo used to be the reason for this choice and is not any more; it has a section of its own below.</p><p><code>toolbar</code> takes <code>true</code>, <code>false</code>, or the controls to draw and the order to draw them in:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toolbar</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mode&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;separator&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;bold&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;italic&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;link&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]} /&gt;</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Item</th><th></th></tr></thead><tbody><tr><td><code>&#39;mode&#39;</code></td><td>The surface switch</td></tr><tr><td><code>&#39;heading&#39;</code></td><td>A menu of heading 1, 2, 3 and body text</td></tr><tr><td><code>&#39;bold&#39;</code>, <code>&#39;italic&#39;</code>, <code>&#39;strikethrough&#39;</code>, <code>&#39;code&#39;</code>, <code>&#39;link&#39;</code>, <code>&#39;image&#39;</code></td><td>Inline formatting</td></tr><tr><td><code>&#39;quote&#39;</code>, <code>&#39;bulletList&#39;</code>, <code>&#39;orderedList&#39;</code>, <code>&#39;taskList&#39;</code>, <code>&#39;codeBlock&#39;</code>, <code>&#39;rule&#39;</code></td><td>Blocks</td></tr><tr><td><code>&#39;find&#39;</code></td><td>Opens the find bar. See <a href="#finding">finding</a></td></tr><tr><td><code>&#39;open&#39;</code>, <code>&#39;save&#39;</code></td><td>Reads a Markdown file in and writes one out. See <a href="#opening-and-saving">opening and saving</a></td></tr><tr><td><code>&#39;colorScheme&#39;</code></td><td>Light, dark, or whatever the system says</td></tr><tr><td><code>&#39;separator&#39;</code></td><td>A hairline, for grouping</td></tr></tbody></table><div class="mawy-fw" data-fw="react"><p><strong>A bar too narrow for its buttons keeps what fits and puts the rest in a menu at the end of it.</strong> It moves whole groups at a time, from the end, and the separators are what mark the groups. So reordering <code>toolbar</code> also reorders what moves first. The surface switch never moves into the menu, because it is the control a writer reaches for most.</p><p>The toolbar decides this by measuring itself; it is not configurable. It replaced a second row, which the layout above the toolbar had no room for: the buttons kept coming, the bar did not grow, and the lower row spilled outside it.</p></div><div class="mawy-fw" data-fw="flutter"><p><strong>A bar too narrow for its buttons scrolls sideways.</strong> Nothing is hidden in a menu. The row is inside a <code>SingleChildScrollView</code>, so what does not fit is a drag away, and the arrows that move the focus along the row scroll it into view as they go.</p><p>The React package solves the same problem with a menu at the end of the bar, and the difference comes from the platform. A row that scrolls under a finger suits a touch screen; a menu suits a page with a pointer and no obvious way to drag a bar sideways.</p></div><h2 id="images" tabindex="-1">Images <a class="header-anchor" href="#images" aria-label="Permalink to &quot;Images&quot;">​</a></h2><p>There are three ways in, because an image can arrive with a URL already or without one.</p><p><strong>The toolbar&#39;s image button</strong> writes <code>![](url)</code> with the destination selected, ready to be typed over. It is the link button with a <code>!</code> in front of it and follows the same rules: select a URL first and it becomes the destination, select anything else and it becomes the description for a reader who is not seeing the image. It needs nothing from the application and is always available.</p><p><strong>An image pasted or dropped as part of a web page</strong> arrives as the URL it already had. That is not an image feature but markup, which <a href="#pasting">pasting</a> reads. Nothing is uploaded, because the picture is already on the web.</p><p><strong>A file</strong> is different. A screenshot on the clipboard or an image dragged in from the desktop needs somewhere for its bytes to go, and that is the one thing this library cannot decide. Whether an image belongs in an object store, behind an upload endpoint, or inline as a <code>data:</code> URI carries cost and policy, so the application decides. It is a prop:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  onUploadImage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(file);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> url;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><p>Answer with the URL to write, or with <code>{ url, alt, title }</code> to say what goes around it. Otherwise the file&#39;s own name, without its extension, becomes the description. Throw, or answer with nothing, and the editor says the image could not be added and writes nothing at all.</p><p><strong>Without <code>onUploadImage</code>, a dropped file is refused.</strong> It is not half-inserted, and not handed back to the browser either: the editor takes the drop, writes nothing, and says so on the line under the document. That is the intended default. The only alternative is turning a two-megabyte screenshot into a <code>data:</code> URI inside somebody&#39;s document.</p><p>The image lands where it was put: a drop goes to the point the pointer let go of it, and a paste goes to the caret. Several files dropped together are uploaded one after another and then written as <strong>one</strong> edit, so a single <code>Mod</code>+<code>Z</code> takes back the whole drop.</p><h2 id="colour-in-the-preview" tabindex="-1">Colour in the preview <a class="header-anchor" href="#colour-in-the-preview" aria-label="Permalink to &quot;Colour in the preview&quot;">​</a></h2><p><code>highlight</code> is passed straight through to the viewer inside the preview, so a <code>split</code> or <code>preview</code> surface colours its code the way <a href="./viewer#colouring-a-code-block">the viewer does</a>. The lazy form works here too, and is the one to use:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  highlight</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mawy-react/highlight&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.mawyHighlighter)}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><p>The <strong>drawn document is not coloured</strong>, and will not be. The source surface has a highlighter of its own for the Markdown. On the drawn surface every caret has to map back into the source, and a second reading of the characters inside a code block would break that mapping.</p><h2 id="opening-and-saving" tabindex="-1">Opening and saving <a class="header-anchor" href="#opening-and-saving" aria-label="Permalink to &quot;Opening and saving&quot;">​</a></h2><p><code>open</code> reads a Markdown file into the editor, and <code>save</code> writes the document back out. <code>Mod</code>+<code>S</code> saves as well, because the browser&#39;s own <code>Mod</code>+<code>S</code> saves the page and that is not what somebody writing in an editor expects.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  onSave</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> api.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`/documents/\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, value)}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><p>Without <code>onSave</code> the text is handed to the browser as a download. It uses an anchor with a <code>download</code> attribute rather than the File System Access API, which only Chromium has, because a save that works in one browser and does nothing in another is worse than one that behaves the same everywhere. With <code>onSave</code>, nothing is downloaded and the application is given the document and the name it would have been saved as.</p><p>The name is the file&#39;s own, when one was opened. Otherwise it is <strong>the document&#39;s first heading</strong>, which is what somebody looking through a folder of these would want to read. Characters no filesystem will take are dropped, and the name is <code>document.md</code> when there is no heading to take it from.</p><p><code>open</code> has no shortcut on purpose. The browser&#39;s own <code>Mod</code>+<code>O</code> is worth leaving alone, and opening a file is not something done in the middle of writing.</p><p><strong>A file dropped on the editor is treated as an image, never as a document.</strong> The drop is already the way an image gets in, and there is a larger reason as well: replacing a document somebody has been writing because a file landed on it loses their work. The viewer opens what is dropped on it because a viewer has nothing to lose. The editor does not.</p><p>The editor still handles the drop. <strong>A file it will not take is refused rather than left to the browser</strong>, which would open it as a page and take the document, the undo history and the caret with the tab. The line under the document says what happened and which control does what was being asked for.</p><p><strong><code>fileDrop</code> turns that rule off</strong>, for an editor that starts empty and is a place to bring a file <em>to</em> rather than one a document already lives in. With it on, a dropped <code>.md</code> opens as the document, and the empty preview shows the viewer&#39;s empty state with its file picker in the pane where the document will appear. A dropped image is still an image wherever <code>onUploadImage</code> is given, and only the files that were being refused are read as documents.</p><div class="mawy-fw" data-fw="flutter"><p>This section applies to the React package only. Here the application opens files and writes them back: a file picker is a plugin rather than a widget, and which one an app has already chosen is not a Markdown editor&#39;s decision. <code>MawyEditorToolbarItem</code> has no <code>open</code> or <code>save</code> for the same reason. <code>onChange</code> hands over the document, and the application decides where it goes.</p></div><h2 id="finding" tabindex="-1">Finding <a class="header-anchor" href="#finding" aria-label="Permalink to &quot;Finding&quot;">​</a></h2><p><code>Mod</code>+<code>F</code> opens the find bar over the source, and the toolbar&#39;s <code>find</code> button does the same thing. <code>Enter</code> goes to the next match, <code>Shift</code>+<code>Enter</code> to the one before, and <code>Escape</code> closes the bar and gives the focus back to the document.</p><div class="mawy-fw" data-fw="react"><p>It exists because the browser&#39;s own find cannot reach here. <strong>No browser searches the text inside a <code>&lt;textarea&gt;</code></strong>, and the source surface is one. Everywhere else in this library a thing the platform already does is left to the platform, and this is the one exception.</p></div><div class="mawy-fw" data-fw="flutter"><p>It exists for the same reason as in the React package. <strong>A platform&#39;s own find reaches a page of text and not the inside of a text field</strong>, and the source surface is a text field. Everywhere else in this library a thing the platform already does is left to the platform, and this is the one exception.</p><p>The arithmetic under it matches the React package function for function: <code>findMatches</code>, <code>matchFrom</code>, <code>replaceMatch</code> and <code>replaceAll</code>. <code>tool/parity.dart</code> diffs the two over <code>tool/searches.json</code> on every change. Whether <code>aa</code> in <code>aaaa</code> is two matches or three is a choice, and both packages make it the same way.</p></div><p>Whatever was selected is already in the box when it opens, as long as it was on one line. That is nearly always what somebody is about to look for.</p><p><strong>Plain text, never a regular expression.</strong> A Markdown document is full of <code>*</code>, <code>[</code>, <code>.</code> and <code>+</code>, and reading those as a pattern turns a single <code>(</code> into a syntax error. There is a case-sensitivity switch instead, which is the one people reach for.</p><p>Replace and replace all are on the second row. Replace all is one pass over the document as it was, so replacing <code>a</code> with <code>aa</code> replaces each <code>a</code> once and never finds its own replacement.</p><div class="mawy-fw" data-fw="react"><p>The bar is only offered where there is a source to search, which is <code>plain</code> and <code>split</code>. In <code>preview</code> and <code>wysiwyg</code> the document is drawn as elements, and the browser&#39;s own find works on it.</p></div><div class="mawy-fw" data-fw="flutter"><p>The bar is only offered where there is a source to search, which is <code>plain</code> and <code>split</code>. In <code>preview</code> there is no field to search, so the button is not drawn.</p></div><h2 id="indenting" tabindex="-1">Indenting <a class="header-anchor" href="#indenting" aria-label="Permalink to &quot;Indenting&quot;">​</a></h2><p>On the source surface, <code>Tab</code> indents and <code>Shift</code>+<code>Tab</code> takes it back. With nothing selected it puts two spaces in where the caret is. With anything selected it moves the lines that selection touches and leaves them selected, so it can be pressed again. No editor still replaces a selected paragraph with a tab character.</p><p>The width is <strong>two spaces</strong>, which Markdown requires. A nested list item has to clear its parent&#39;s marker, and under <code>- </code> that is two columns. Four would become an indented code block the moment the list above it ends. Going back takes a tab or up to two spaces off the front of each line, and a line with nothing left to take is not an error. The rest of the block still moves.</p><p>Capturing <code>Tab</code> in a textarea creates a keyboard trap: somebody who cannot use a pointer would have no way to leave the editor. So there is a key that lets you out.</p><p><strong>Press <code>Escape</code>, then <code>Tab</code>, and the focus moves on.</strong> One <code>Escape</code> arms it and anything else typed disarms it again. It is the rule CodeMirror, Monaco and GitHub&#39;s own editor all use, so anybody who has met one of those already knows it. The surface also announces it to a screen reader.</p><p>The drawn document does not capture <code>Tab</code> at all. It is one focusable element, so <code>Tab</code> moves straight out of it.</p><h2 id="undo" tabindex="-1">Undo <a class="header-anchor" href="#undo" aria-label="Permalink to &quot;Undo&quot;">​</a></h2><p><code>Mod</code>+<code>Z</code> goes back, <code>Mod</code>+<code>Shift</code>+<code>Z</code> comes forward again, and <code>Ctrl</code>+<code>Y</code> is the Windows spelling of the same command. The history is <strong>one list for the whole editor</strong> rather than one per surface.</p><p>On its own, the source surface could have used the browser&#39;s own stack, which a <code>&lt;textarea&gt;</code> keeps well. But the drawn document is a <code>contenteditable</code> that refuses every input, so nothing is ever recorded on the browser&#39;s stack. With two stacks, an edit made in <code>wysiwyg</code> and taken back in <code>plain</code> would step through half of what happened and then stop.</p><p>What is stored is the document before each change together with where the caret was, because that is the state undo has to arrive at.</p><p>A run of typing is <strong>one step</strong>, not one per keystroke, because an undo that gives back one character at a time is not worth pressing. A change carries on from the one before it while it is the same kind of change, in the same place, within a moment of it. A syllable being composed counts as more of the same typing, because a Korean keyboard rewrites what it wrote on every jamo and none of those are separate edits. A line ending closes the run behind it: what is typed after <code>Enter</code> is a new thought, so undo stops between the two.</p><div class="mawy-fw" data-fw="flutter"><p><strong>This section applies to the React package only, and this package keeps no history of its own.</strong> The reason that one needs a history does not arise here. There are two editable surfaces over there and the drawn one gets no entry on the browser&#39;s stack, so a single list is the only way an edit made on one can be taken back on the other. Here there is one editable surface, an <code>EditableText</code>, and it keeps its own undo stack.</p><p>So undo is Flutter&#39;s rather than this package&#39;s, and it behaves the way it does in every other text field in your application, which is what somebody typing expects. How it treats a change made by a toolbar button rather than a keystroke is also Flutter&#39;s to decide, and it matches any other <code>TextEditingController</code> written to from outside.</p></div><h2 id="pasting" tabindex="-1">Pasting <a class="header-anchor" href="#pasting" aria-label="Permalink to &quot;Pasting&quot;">​</a></h2><p><strong>What is on the clipboard as HTML arrives as Markdown.</strong> Copy a section of a web page into either surface and the headings are hashes, the links are links, the list is a list. Copy out of a word processor and the same is true.</p><p>A clipboard with nothing but text on it is left to the browser. Its own paste is already correct, and letting it happen keeps the caret, the scroll and the run of undo where they were.</p><p>This is <strong>not</strong> the renderer run backwards. Markup from somewhere else is read once, for whatever can be made of it, and no document round-trips through it. So it is lossy: a <code>&lt;span style=&quot;color: red&quot;&gt;</code> becomes its text, a <code>&lt;video&gt;</code> becomes nothing, and an attribute that is not on the list is dropped. Every URL goes through the same check a Markdown link gets, so a pasted <code>javascript:</code> link arrives as the words it was written with.</p><p>Inside a code block a paste is plain text and nothing else. Everything in there is the characters it is, so a pasted heading is a line beginning with a hash.</p><div class="mawy-fw" data-fw="flutter"><p>This section applies to the React package only. A clipboard here holds what the platform says it holds, and reaching past plain text to the HTML flavour of it, or to an image, needs a plugin rather than a widget. So a paste is the platform&#39;s paste, and an application can turn a page of HTML into Markdown before handing the string over.</p></div><h2 id="the-status-bar" tabindex="-1">The status bar <a class="header-anchor" href="#the-status-bar" aria-label="Permalink to &quot;The status bar&quot;">​</a></h2><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;position&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;words&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;size&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]} /&gt;</span></span></code></pre></div><p>Pick from <code>&#39;position&#39;</code>, <code>&#39;selection&#39;</code>, <code>&#39;lines&#39;</code>, <code>&#39;words&#39;</code>, <code>&#39;characters&#39;</code> and <code>&#39;size&#39;</code>, or pass <code>false</code> for none.</p><p>Two of those are worth explaining. <strong>Characters</strong> are code points, so an emoji counts as one. <strong>Words</strong> add every Han, hiragana and katakana character to the space-separated count, because those languages are written without spaces and a whitespace split would call a page of them one word. Korean is spaced, so an eojeol counts as one word. <strong>Size</strong> is UTF-8 bytes, which is the size of the file on disk and differs from the character count as soon as anything is not ASCII.</p><h2 id="the-language-of-the-interface" tabindex="-1">The language of the interface <a class="header-anchor" href="#the-language-of-the-interface" aria-label="Permalink to &quot;The language of the interface&quot;">​</a></h2><p>The toolbar&#39;s labels and tooltips, the surface switch, the words in the status bar, the find bar, the text an empty editor shows and everything said to a screen reader are written by the library. <code>locale</code> sets which language they are in.</p><div class="mawy-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">locale</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ko&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div></div><div class="mawy-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyEditor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(defaultValue</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document, locale</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyLocale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.ko);</span></span></code></pre></div></div><p><strong>English and Korean</strong>, and <code>en</code> is the default. It is the same prop the viewer takes and means the same thing here. <a href="./viewer#the-language-of-the-interface">The language of the interface</a> has the rest, including what adding a language involves.</p><p>The editor passes it to the viewer inside the preview, so the source surface and the drawn one are never in two languages. It says nothing about the document itself: an English-interface editor with a Korean document in it is a common case, so the two settings are kept separate.</p><h2 id="accessibility" tabindex="-1">Accessibility <a class="header-anchor" href="#accessibility" aria-label="Permalink to &quot;Accessibility&quot;">​</a></h2><p>The editing surface is a labelled <code>textbox</code>, and everything the toolbar can do is reachable from the keyboard. The toolbar has the ARIA <code>toolbar</code> role: one tab stop, arrow keys inside. <code>Tab</code> indents the source rather than moving the focus, so <code>Escape</code> and then <code>Tab</code> is the way out. See <a href="#indenting">indenting</a> for why.</p>`,99))])}const y=i(h,[["render",r]]);export{w as __pageData,y as default};
