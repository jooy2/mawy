import{_ as h,C as o,o as r,c as l,j as s,a as n,E as t,a2 as a}from"./chunks/framework.Cd7mb1YL.js";const m=JSON.parse('{"title":"Viewer","description":"The viewer renders a Markdown document and does not edit it.","frontmatter":{"title":"Viewer","order":3},"headers":[],"relativePath":"guide/viewer.md","filePath":"en/guide/viewer.md","lastUpdated":1788614595000}'),d={name:"guide/viewer.md"},p={class:"mawy-fw","data-fw":"react"};function c(k,e,g,y,E,u){const i=o("MawyDemo");return r(),l("div",null,[e[2]||(e[2]=s("h1",{id:"the-viewer",tabindex:"-1"},[n("The viewer "),s("a",{class:"header-anchor",href:"#the-viewer","aria-label":'Permalink to "The viewer"'},"​")],-1)),e[3]||(e[3]=s("p",null,"The viewer renders a Markdown document and does not edit it.",-1)),t(i,{name:"viewer/basic",flutter:"viewer/basic",height:520}),e[4]||(e[4]=a(`<div class="mawy-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MawyViewer } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;mawy-react&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Page</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">document</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">document</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} /&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></div><div class="mawy-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;package:mawy/mawy.dart&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document);</span></span></code></pre></div></div><p>That is the whole of it. There is no theme object to fill in, nothing to register before a document renders, and no second library to do the rendering.</p><h2 id="editor-and-viewer-in-one-package" tabindex="-1">Editor and viewer in one package <a class="header-anchor" href="#editor-and-viewer-in-one-package" aria-label="Permalink to &quot;Editor and viewer in one package&quot;">​</a></h2><p>When the editor and the viewer use different Markdown implementations, one failure keeps happening: an author writes a document in the editor, it looks right there, and it renders differently for the reader. Every difference between the two implementations causes it, whether that is how a list nests, whether a line break is a break, or what an unclosed emphasis does.</p><p>Sharing the parser removes that failure. What the author saw in <code>preview</code> is what the viewer draws, because both go through the same code path.</p><div class="mawy-fw" data-fw="flutter"><p>That holds across the two packages as well, and it is checked. The Dart parser and the TypeScript one are the same files, the same functions and the same rules, and <code>packages/flutter/tool/parity.dart</code> runs both over every Markdown file in the repository and diffs the trees. A document that means one thing in a browser means the same thing in an app.</p></div><h2 id="using-it-without-value" tabindex="-1">Using it without <code>value</code> <a class="header-anchor" href="#using-it-without-value" aria-label="Permalink to &quot;Using it without \`value\`&quot;">​</a></h2><div class="mawy-fw" data-fw="flutter"><p>The Flutter package does not do this; <code>value</code> is required there. Opening a file needs a file-picker plugin, which this package does not include and an application usually already has. Your application reads the file and Mawy draws it, so this whole section applies to the React package only.</p></div>`,9)),s("div",p,[e[0]||(e[0]=s("p",null,[s("code",null,"value"),n(" is optional. With no document to show, the viewer becomes a file picker: drop a "),s("code",null,".md"),n(" file on it, or choose one.")],-1)),t(i,{name:"viewer/empty"}),e[1]||(e[1]=a(`<p>Which half you get follows from which props you pass:</p><table tabindex="0"><thead><tr><th>You pass</th><th>The viewer</th><th>Dropping a file</th></tr></thead><tbody><tr><td>nothing</td><td>opens whatever it is given</td><td>keeps it, and calls <code>onValueChange</code></td></tr><tr><td><code>defaultValue</code></td><td>starts there, then keeps its own</td><td>keeps it, and calls <code>onValueChange</code></td></tr><tr><td><code>value</code></td><td>shows what you pass, always</td><td>is off. Pass <code>fileDrop</code> to turn it back on</td></tr></tbody></table><p><code>onValueChange</code> is called either way, with the text and the <code>File</code> it came from:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  onValueChange</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">markdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(file?.name, markdown.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><p>A file larger than five megabytes is refused rather than read. That is about a million words of Markdown, and the limit prevents a browser tab from freezing because somebody dropped a database dump on it.</p><p><strong>A viewer that cannot be given a document does not offer to open one.</strong> <code>value</code> with no <code>onValueChange</code> means the application owns the document, so a file chosen here would have nowhere to go. The empty state says there is nothing to show yet, the button under it is not drawn, and the toolbar&#39;s <code>open</code> is disabled. That avoids a control that does nothing when it is pressed.</p>`,6))]),e[5]||(e[5]=a(`<div class="mawy-fw" data-fw="flutter"><p><strong>A mouse wheel notch is applied over several frames.</strong> By default Flutter moves the offset to where the notch says on the next frame and draws nothing in between, which is why the same document scrolls less smoothly in Flutter than in a browser. Every browser and every native application on these platforms animates it, so this package does too. There is nothing to turn on, and a reader who has asked the platform for less movement gets the immediate jump back, which is what the stylesheet does under <code>prefers-reduced-motion</code>.</p><p>The source surface keeps the platform&#39;s own behaviour. A text field scrolls itself rather than being scrolled by the widget around it, so there is nothing to insert between them.</p><p><strong>A reader can select and copy the document&#39;s text.</strong> Drag across it to select, and <code>Ctrl</code>/<code>Cmd</code>+<code>C</code> to copy. This is worth writing down because it had to be built: a document drawn as widgets selects nothing unless it is placed inside a region that allows it. There are no selection handles and no context menu, because both come from Material or Cupertino and this package uses neither. So the shortcuts are written out here, the same as <code>Enter</code> and the space bar.</p><p><strong>A document past four hundred blocks is built only where it can be seen.</strong> Under that, every block is built however tall the document is. A selection can only take text that has been built, so the whole document stays selectable at that size. Over it, blocks come and go as the reader scrolls, and the cost of drawing the document, laying it out again when the type changes, and holding it in memory stops growing with its length. A document of two thousand four hundred blocks went from twenty-five thousand render objects to five hundred, and from a second to a tenth of one to lay out again after a change of type.</p><p>Four hundred blocks is past a long README, a reference page or a chapter. Past it, a selection reaches the three screens either way that the list keeps, and no further. The toolbar&#39;s copy button takes the whole document from the Markdown rather than from the page, so copying all of a long document does not go through a selection. None of this is configurable. Where every block sits is recorded as it is laid out, which lets the outline, the find bar and <code>anchors</code> keep working for blocks that are not on the screen.</p></div><h2 id="supported-syntax" tabindex="-1">Supported syntax <a class="header-anchor" href="#supported-syntax" aria-label="Permalink to &quot;Supported syntax&quot;">​</a></h2><p>CommonMark, and GitHub&#39;s additions on top of it:</p><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Blocks</strong></td><td>ATX and setext headings, paragraphs, fenced and indented code, block quotations, ordered and bullet lists to any depth, thematic breaks, HTML blocks</td></tr><tr><td><strong>Inline</strong></td><td>emphasis, strong, <code>code</code>, links, images, autolinks, hard line breaks, character references, backslash escapes</td></tr><tr><td><strong>GitHub</strong></td><td>tables with per-column alignment, task lists, <code>~~strikethrough~~</code>, bare URLs and e-mail addresses, footnotes, and the five <a href="https://docs.github.com/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax#alerts" target="_blank" rel="noreferrer">alert</a> kinds</td></tr><tr><td><strong>References</strong></td><td><code>[label]: url &quot;title&quot;</code> definitions, resolved wherever in the file they are written</td></tr><tr><td><strong>And one more</strong></td><td>definition lists, which GitHub does not read</td></tr></tbody></table><p><code>parse</code> is where the options live:</p><div class="mawy-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parse</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{{ gfm: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, breaks: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, definitionLists: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }} /&gt;</span></span></code></pre></div></div><div class="mawy-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  parse</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyParseOptions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(gfm</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, breaks</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, definitionLists</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div></div><ul><li><strong><code>gfm</code></strong> (default <code>true</code>) — GitHub&#39;s additions. Off, a <code>|</code> is a pipe and <code>~~</code> is four tildes.</li><li><strong><code>breaks</code></strong> (default <code>false</code>) — whether a single newline inside a paragraph is a line break. The CommonMark specification says it is not, while chat clients and issue trackers treat it as one. A reader who has never written Markdown expects the latter, so this is an option rather than a fixed behaviour.</li><li><strong><code>definitionLists</code></strong> (default <code>true</code>) — whether <code>: </code> under a line of text is a term and what it means. See below.</li></ul><h3 id="commonmark-coverage" tabindex="-1">CommonMark coverage <a class="header-anchor" href="#commonmark-coverage" aria-label="Permalink to &quot;CommonMark coverage&quot;">​</a></h3><p><strong>640 of the specification&#39;s 652 examples</strong>, run against the parser on every change. CommonMark ships a test suite with the specification, so the coverage can be stated as a number. It is in <code>packages/react/test/internal/markdown/commonmark.test.ts</code>, beside the list of the other 12.</p><p>Three of those are deliberate rather than missing: every URL is checked against a scheme allowlist, so <code>&lt;made-up-scheme://foo&gt;</code> is drawn as the words the author wrote. Five more are the same kind of choice: an empty destination is drawn as the author&#39;s words rather than as an <code>&lt;a href=&quot;&quot;&gt;</code> that does nothing. Most of the rest are edge cases, such as a tab inside a list item, a character reference the table does not carry, or a reference definition alone in a list item. Each one is written down with the reason it is there, so the list only gets shorter on purpose.</p><p>The Dart parser does not need to be run against the suite. The two parsers&#39; trees are diffed over every awkward case and every Markdown file in the repository, so a tree that is right in one is the tree the other produces.</p><p><strong>Containers nest a hundred deep.</strong> Past that nothing opens: the lines become the paragraphs they would be with no rules applied, and the markers on them stay as characters. Every container reads its own inside, so nesting costs a stack of calls as deep as the document. <code>&gt; </code> written a couple of thousand times is a four-kilobyte file with nothing in it, and anybody can send one to an application that draws documents from elsewhere. A hundred is deeper than anything written by hand and well short of the stack limit.</p><h3 id="footnotes" tabindex="-1">Footnotes <a class="header-anchor" href="#footnotes" aria-label="Permalink to &quot;Footnotes&quot;">​</a></h3><p>A <code>[^label]</code> in a sentence becomes a number, and the note it points at is drawn under the document with a link back to where it was mentioned:</p><div class="language-md vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Mawy parses its own Markdown.[</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">^why</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">^why</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]: Only the parser can say where a piece of the document came from, and everything else here is built on that.</span></span></code></pre></div><p>Three things are worth knowing, and all three are what GitHub does:</p><ul><li><strong>They are numbered by the order they are first mentioned</strong>, not the order they are written in. The reader meets <code>1</code> before <code>2</code> whatever the file looks like.</li><li><strong>A note nobody mentions is not drawn at all.</strong> It is a note to the author, the same way a <code>[label]: url</code> nobody links to is.</li><li><strong>A <code>[^label]</code> with nothing to point at stays as the characters it was written with</strong>, rather than becoming a link to nowhere.</li></ul><p>A note may be a whole run of blocks, such as a second paragraph, a list or a code block, as long as the lines after the first are indented four spaces. Where it was written does not matter: the parser lifts it out of the flow, so a note in the middle of a section is still drawn at the bottom.</p><h3 id="definition-lists" tabindex="-1">Definition lists <a class="header-anchor" href="#definition-lists" aria-label="Permalink to &quot;Definition lists&quot;">​</a></h3><p>This is the one syntax here that GitHub does not read. It follows <a href="https://michelf.ca/projects/php-markdown/extra/#def-list" target="_blank" rel="noreferrer">PHP Markdown Extra</a>:</p><div class="language-md vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Markdown : A way of writing that reads as what it says.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Mawy : This. : And the editor beside it.</span></span></code></pre></div><p>A term is a line of text, and what it means is a line opening with a colon <strong>and a space</strong>. Requiring the space is what keeps <code>:warning:</code> under a sentence from turning that sentence into a term. Several terms may share a meaning, several meanings may share a term, and a meaning may be a whole run of blocks if the lines after the first are indented. A blank line before a meaning spaces the whole list out, exactly as it does in a bullet list.</p><p>Pass <code>definitionLists: false</code> in <code>parse</code> to turn it off, for a document that has to mean exactly what it would mean on GitHub.</p><h2 id="colouring-a-code-block" tabindex="-1">Colouring a code block <a class="header-anchor" href="#colouring-a-code-block" aria-label="Permalink to &quot;Colouring a code block&quot;">​</a></h2><div class="mawy-fw" data-fw="flutter"><p>Nothing is coloured by default, and that is deliberate. A highlighter is the largest piece a Markdown renderer can carry, and most documents have nothing to colour. So it is an argument, and an application that never references one never carries the grammars behind it, because a Dart build drops unreferenced code:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document, highlight</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mawyHighlighter);</span></span></code></pre></div><p><code>mawyHighlighter</code> is the React package&#39;s highlighter in Dart. <code>lib/src/highlight.dart</code> matches <code>src/highlight.ts</code> rule for rule, and <code>tool/parity.dart</code> diffs every token the two produce over a piece of every language either of them supports. So a code block coloured in a browser is coloured the same way in an app, the same guarantee the parser gives.</p><p>The supported languages are the ones a document usually shows: <code>js</code>, <code>ts</code>, <code>jsx</code>, <code>tsx</code>, <code>json</code>, <code>html</code>, <code>xml</code>, <code>css</code>, <code>bash</code>, <code>python</code>, <code>yaml</code>, <code>sql</code>, <code>dart</code>, <code>go</code>, <code>rust</code>, <code>java</code>, <code>c</code>, <code>cpp</code> and the other names each of those answers to. The result is <strong>approximate</strong>, deliberately and permanently. A template literal with a brace in it or a regular expression that reads as division comes out slightly wrong, which does not matter, because colour does not have to be exact.</p><p>For anything more than that, <code>MawyHighlighter</code> is the whole interface and any grammar behind it is a few lines:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MyHighlighter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyHighlighter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MyHighlighter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  @override</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  bool</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> supports</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> language) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> language </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;dart&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  @override</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  List</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyCodeToken</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">highlight</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> code, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> language) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tokensFor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(code);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A highlighter returns <strong>tokens rather than markup</strong>, following the same principle as the rest of the library. It hands back text and names: <code>keyword</code>, <code>string</code>, <code>comment</code> and about ten more. This package decides what each one becomes, so nothing reaches the screen as markup and a highlighter that is wrong cannot add anything to a document.</p><p>A highlighter has one requirement: joining its tokens back together <strong>must</strong> reproduce the code. What comes back is joined and checked against what went in, and a block whose tokens do not add up is drawn plain. Losing the colour is better than showing something the document does not say.</p><p>The colours themselves are eight fields on <code>MawyTokens</code>: <code>highlightComment</code>, <code>highlightString</code>, <code>highlightNumber</code>, <code>highlightKeyword</code>, <code>highlightType</code>, <code>highlightFunction</code>, <code>highlightVariable</code> and <code>highlightPunctuation</code>. They are the same eight the React package declares as <code>--mawy-hl-*</code>, value for value.</p></div><div class="mawy-fw" data-fw="react"><p>Nothing is coloured by default, and that is deliberate. A highlighter is the largest piece a Markdown renderer can carry, and most documents have nothing to colour. So it is a prop, and the prop takes a <strong>function</strong>, so nothing is fetched until a document with a language on a fence is drawn:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  highlight</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mawy-react/highlight&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.mawyHighlighter)}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><p><code>mawy-react/highlight</code> is a separate entry point, so an application that never references it never ships it. It holds Mawy&#39;s own highlighter, for the languages a document usually shows: <code>js</code>, <code>ts</code>, <code>jsx</code>, <code>tsx</code>, <code>json</code>, <code>html</code>, <code>xml</code>, <code>css</code>, <code>bash</code>, <code>python</code>, <code>yaml</code>, <code>sql</code>, <code>dart</code>, <code>go</code>, <code>rust</code>, <code>java</code>, <code>c</code>, <code>cpp</code> and the other names each of those answers to. The result is <strong>approximate</strong>, deliberately and permanently. A template literal with a brace in it or a regular expression that reads as division comes out slightly wrong, which does not matter, because colour does not have to be exact.</p><p>For anything more than that, <code>MawyHighlighter</code> is the whole interface and Shiki or Prism behind it is a few lines:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> shiki</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MawyHighlighter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  supports</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">language</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> languages.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">includes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(language),</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  highlight</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">code</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">language</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> toMawyTokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> codeToTokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(code, { lang: language }))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>A highlighter returns <strong>tokens rather than markup</strong>, following the same principle as the rest of the library. It hands back text and names: <code>keyword</code>, <code>string</code>, <code>comment</code> and about ten more. This package decides which element each one becomes, so nothing reaches the page as a string of HTML and a highlighter that is wrong cannot put a <code>&lt;script&gt;</code> in a document. A name that is not on the list is drawn as plain text.</p><p>A highlighter has one requirement: joining its tokens back together <strong>must</strong> reproduce the code. What comes back is joined and checked against what went in, and a block whose tokens do not add up is drawn plain. Losing the colour is better than showing something the document does not say.</p><p>Each coloured piece carries its source range, like everything else the viewer draws, so a click in the middle of a code block still finds the character it landed on.</p><p>The colours themselves are eight custom properties: <code>--mawy-hl-comment</code>, <code>--mawy-hl-string</code>, <code>--mawy-hl-number</code>, <code>--mawy-hl-keyword</code>, <code>--mawy-hl-type</code>, <code>--mawy-hl-function</code>, <code>--mawy-hl-variable</code> and <code>--mawy-hl-punctuation</code>. They are declared on <code>.mawy-root</code> in both palettes, and an application can redeclare them.</p></div><h2 id="directives" tabindex="-1">Directives <a class="header-anchor" href="#directives" aria-label="Permalink to &quot;Directives&quot;">​</a></h2><p>A viewer draws what Markdown can express, and a document sometimes needs something Markdown has no syntax for: a video, a formula, the house callout every page on your site uses. There are two obvious ways to handle that, and both have problems. Raw HTML is the one thing the library&#39;s safety rests on not needing. A library that knows about videos then has to know about everything else too.</p><p>A directive is the third way. The parser reads a <strong>shape</strong> and stops there. It does not interpret what <code>youtube</code> is, which is what lets a document carry one, and the application decides what the shape means.</p><p>There are three kinds, and the number of colons tells them apart:</p><div class="language-md vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:::callout[</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">Careful</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]{kind=warning} Blocks, and they are parsed as blocks: </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**emphasis**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, lists, code. :::</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::youtube{id=dQw4w9WgXcQ}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Press :kbd[</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">Ctrl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] to go.</span></span></code></pre></div><p>A container holds blocks and closes on colons of its own length or more, so <code>::::</code> can hold a <code>:::</code>. A leaf is a single line with nothing under it. A text directive sits inside a sentence.</p><div class="mawy-fw" data-fw="react"><p>Which component each name becomes is one prop:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Callout</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">attributes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">label</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">children</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MawyDirectiveProps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">aside</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> className</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`callout callout-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">attributes</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;note&#39;}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {label </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">h3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;{label}&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">h3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {children}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">aside</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">directives</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{{ callout: Callout, youtube: YouTube }} /&gt;;</span></span></code></pre></div><p>A component is handed the <code>name</code>, the <code>attributes</code>, the <code>label</code> already drawn, a container&#39;s <code>children</code> already drawn, the <code>range</code> it was written at and the <code>source</code> it was written with. So it composes React elements and never sees a string of markup. The safety guarantee holds across this extension point: there is still no <code>innerHTML</code> between the Markdown and the page, and a directive that draws something dangerous was written by the application.</p></div><div class="mawy-fw" data-fw="flutter"><p>Which widget each name becomes is one argument:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  directives</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyDirectiveBuilder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;callout&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">BuildContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> context, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyDirective</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> directive) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Callout</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> directive.attributes[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;kind&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;note&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> directive.label,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      children</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> directive.children</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>A builder is handed the <code>name</code>, the <code>attributes</code>, the <code>label</code> already drawn as an <code>InlineSpan</code>, a container&#39;s <code>children</code> already drawn as widgets, the <code>range</code> it was written at and the <code>source</code> it was written with. So it composes widgets and never sees markup of any kind. A text directive is placed in the sentence as a <code>WidgetSpan</code>, so its builder should return something that fits on a line of text.</p></div>`,35)),t(i,{name:"viewer/directives",flutter:"viewer/directives",height:460}),e[6]||(e[6]=a(`<p><code>{…}</code> is written the way it is everywhere else this syntax is used: <code>key=value</code>, <code>key=&quot;a value with spaces&quot;</code>, <code>#id</code> and <code>.a .b</code>, which arrive as <code>id</code> and <code>class</code>. A bare <code>key</code> arrives with an empty string and is how a flag is written. Every value is a string, because a string is all the document supplied. Reading one as a number, and deciding what a missing one means, is the component&#39;s job.</p><p><strong>An unregistered name is drawn as the characters it was written with.</strong> Raw HTML gets the same treatment under the default <code>html</code> policy, for the same reason: a viewer that was never told what a construct means should show what the author wrote rather than quietly drop part of a document. It is also the only fallback that loses nothing, since an unhandled <code>::youtube{id=…}</code> has no content inside it to fall back <em>to</em>.</p><div class="mawy-fw" data-fw="react"><p>On the <code>wysiwyg</code> surface those characters map to the source one for one, so an unregistered directive is editable exactly where it was typed.</p></div><p>Two rules here are narrower than the <a href="https://github.com/remarkjs/remark-directive" target="_blank" rel="noreferrer"><code>remark-directive</code></a> extension this syntax comes from, and both are about not changing what a document already said:</p><ul><li><strong>The colons are followed immediately by the name.</strong> <code>::: tip</code> with a space stays the paragraph it was, so every document that already writes containers that way keeps its meaning.</li><li><strong>An inline directive carries a label or attributes.</strong> <code>:name</code> on its own is not one, so <code>Note:</code> and <code>12:30</code> and <code>:warning:</code> in a sentence stay exactly what they are.</li></ul><p>This parser reads everything else the extension reads, so a document written for one works with the other.</p><h2 id="safety" tabindex="-1">Safety <a class="header-anchor" href="#safety" aria-label="Permalink to &quot;Safety&quot;">​</a></h2><p>A viewer renders content that the person running it did not write, so the default is the safe one.</p><div class="mawy-fw" data-fw="react"><p><strong>The document becomes React elements, not a string of HTML.</strong> There is no <code>innerHTML</code> on the path from Markdown to the page: a node in the parsed document can only become an element the renderer has a <code>case</code> for. Rather than escaping carefully, the structure leaves nothing to escape.</p></div><div class="mawy-fw" data-fw="flutter"><p><strong>The document becomes widgets, never a string.</strong> There is no markup on the path from Markdown to the screen: a node in the parsed document can only become a widget the renderer has a <code>case</code> for. Rather than escaping carefully, the structure leaves nothing to escape.</p></div><div class="mawy-fw" data-fw="react"><p><strong>A link opens in a new tab.</strong> <code>linkTarget</code> is <code>&#39;blank&#39;</code> by default, which puts <code>target=&quot;_blank&quot;</code> and <code>rel=&quot;noopener noreferrer&quot;</code> on it. A viewer is usually a piece of a page rather than the whole page, so a reader who follows a link out and comes back should find the document where they left it, and in an editor there is unsaved work behind that link. <code>linkTarget=&quot;self&quot;</code> is for an application showing a document <em>as</em> its page. A footnote&#39;s number and the arrow back from it point at the same page, so neither is affected.</p></div><p><strong>Every URL is checked, in Markdown as much as in HTML.</strong> <code>[click](javascript:…)</code> is plain Markdown with no HTML anywhere near it, so the scheme allowlist is not part of the HTML option and is not switched off with it. A refused destination is drawn as the words the author wrote, with no link around them, so a reader sees the sentence rather than a link that does nothing.</p><div class="mawy-fw" data-fw="flutter"><p><strong>Raw HTML is shown as the characters it was written with, and there is no option to change that.</strong> Flutter has no HTML to draw it as, which is why the Flutter package has no <code>html</code> prop. The rest of this section applies to the React package only.</p><p><strong>Links are not opened either.</strong> A tapped link does nothing until the application defines what opening one means, through <code>onLinkTap</code>. Handing a URL to the platform is not a viewer&#39;s decision. The scheme allowlist has already run, and the application handles the rest.</p></div><div class="mawy-fw" data-fw="react"><p><strong>Raw HTML inside a document is inert until you ask for it.</strong> <code>html</code> is the one prop that can change that:</p><table tabindex="0"><thead><tr><th><code>html</code></th><th>What a <code>&lt;div&gt;</code> in the document becomes</th></tr></thead><tbody><tr><td><code>&#39;escape&#39;</code> <em>(default)</em></td><td>the characters it was written with, shown as text</td></tr><tr><td><code>&#39;sanitize&#39;</code></td><td>a real <code>&lt;div&gt;</code>, with everything outside an allowlist of elements, attributes and URL schemes removed</td></tr><tr><td><code>&#39;raw&#39;</code></td><td>a real <code>&lt;div&gt;</code>, exactly as written, <strong>including anything in it that runs</strong></td></tr></tbody></table><p><code>&#39;sanitize&#39;</code> puts every name the document gives something under <code>user-content-</code>. An <code>id</code> becomes a global on the page, so <code>&lt;a id=&quot;config&quot;&gt;</code> is <code>window.config</code> in every browser, and a <code>name</code> does the same to <code>document</code>, so <code>&lt;img name=&quot;getElementById&quot;&gt;</code> shadows that method for every script around it. Under the prefix those names collide with nothing the page has. Links the document wrote to its own names move with them. A link to a heading does not, because a heading&#39;s anchor is the author&#39;s own words rather than markup. The prefix is the one GitHub uses, so a document written for GitHub keeps working.</p><p><code>&#39;sanitize&#39;</code> parses with <code>DOMParser</code> rather than with a regular expression, on purpose. HTML&#39;s error recovery is the attack surface, and the only parser that agrees with a browser about what <code>&lt;img src=x onerror=alert(1)&gt;</code> means is a browser&#39;s. Where there is no <code>DOMParser</code>, such as a server render, it shows the markup rather than guessing.</p><p>So the browser&#39;s first paint matches the server&#39;s output, and the elements arrive on the render after it. That is deliberate: drawing them straight away would have React find elements where the server sent characters, which is a hydration mismatch. An application that never renders on a server never hydrates, so its first render is already the browser&#39;s, with nothing deferred and nothing flashing.</p><p><code>&#39;raw&#39;</code> removes nothing and checks nothing. A <code>&lt;script&gt;</code> in the document runs, an <code>onerror</code> on an image runs, and an <code>&lt;iframe&gt;</code> loads, all of it in the page&#39;s own origin, with the page&#39;s own cookies and whatever the signed-in reader can reach. <strong>Anybody who can put characters into the document can do anything the application can do</strong>: read the session, call the API as that reader, and rewrite the page.</p><p>Set it only where the document is the application&#39;s own, or has already been made safe by something upstream that the application trusts. A report about rendering untrusted Markdown with it set is <a href="https://github.com/jooy2/mawy/blob/main/SECURITY.md" target="_blank" rel="noreferrer">out of scope</a> as a vulnerability, because that is the documented meaning of the value. Use <code>&#39;sanitize&#39;</code> for a document that came from somewhere else.</p></div><h2 id="the-toolbar" tabindex="-1">The toolbar <a class="header-anchor" href="#the-toolbar" aria-label="Permalink to &quot;The toolbar&quot;">​</a></h2><p>The toolbar controls how the document is <strong>set</strong>, not what it says. A reader can turn the text up, widen the line spacing, or switch to a serif, and the document underneath is untouched.</p><div class="mawy-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toolbar</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fontSize&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;colorScheme&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]} /&gt;</span></span></code></pre></div></div><div class="mawy-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  toolbar</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewerToolbarItem</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    MawyViewerToolbarItem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.fontSize,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    MawyViewerToolbarItem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.colorScheme,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div></div>`,18)),t(i,{name:"viewer/minimal",flutter:"viewer/minimal",height:360}),e[7]||(e[7]=a(`<div class="mawy-fw" data-fw="react"><p><code>toolbar</code> takes <code>true</code> for all of it, <code>false</code> for none, or the controls to draw and the order to draw them in:</p></div><div class="mawy-fw" data-fw="flutter"><p><code>toolbar</code> takes a list: <code>kMawyViewerToolbar</code> for all of it, <code>const []</code> for none, or the controls to draw and the order to draw them in.</p></div><table tabindex="0"><thead><tr><th>Item</th><th>What it does</th></tr></thead><tbody><tr><td><code>&#39;fontFamily&#39;</code></td><td>whichever typefaces the viewer was given. See <a href="#typefaces">below</a></td></tr><tr><td><code>&#39;fontSize&#39;</code></td><td>13 to 26 pixels</td></tr><tr><td><code>&#39;lineHeight&#39;</code></td><td>1.3 to 2.4</td></tr><tr><td><code>&#39;letterSpacing&#39;</code></td><td>−0.04 to 0.16em</td></tr><tr><td><code>&#39;measure&#39;</code></td><td>how wide the column of text may run</td></tr><tr><td><code>&#39;colorScheme&#39;</code></td><td>light, dark, or whatever the system says</td></tr><tr><td><code>&#39;outline&#39;</code></td><td>opens the headings panel</td></tr><tr><td><code>&#39;copy&#39;</code></td><td>the Markdown source, to the clipboard</td></tr><tr><td><code>&#39;open&#39;</code></td><td>the file picker</td></tr><tr><td><code>&#39;separator&#39;</code></td><td>a hairline, for grouping a long list</td></tr></tbody></table><div class="mawy-fw" data-fw="flutter"><p>Here they are the values of <code>MawyViewerToolbarItem</code>, so the second row is <code>MawyViewerToolbarItem.fontSize</code>. The list is the same except for <code>open</code>, because this package does not open files.</p></div><p>There is no way to add a control that is not on that list, and that is deliberate. A toolbar that takes arbitrary children is one the library can no longer keep keyboard-operable.</p><div class="mawy-fw" data-fw="react"><p>It has the ARIA <code>toolbar</code> role rather than being a plain row of buttons. One Tab enters it and one Tab leaves, and the arrow keys, <code>Home</code> and <code>End</code> move between the controls inside. That way a keyboard-only reader reaches the document in two keystrokes instead of passing through eleven controls.</p></div><div class="mawy-fw" data-fw="flutter"><p>Every control is a named <code>Semantics</code> button that exposes its pressed state, so a screen reader reads it as a toolbar. It is one tab stop, like the React package&#39;s, and the arrows move between the controls inside it. See <a href="#accessibility">accessibility</a> below.</p></div><h2 id="typefaces" tabindex="-1">Typefaces <a class="header-anchor" href="#typefaces" aria-label="Permalink to &quot;Typefaces&quot;">​</a></h2><p>By default the menu offers three, and they are roles rather than font names: <code>sans</code>, <code>serif</code> and <code>mono</code>, drawn with whatever is already on the reader&#39;s machine. Nothing is downloaded and nothing can fail.</p><div class="mawy-fw" data-fw="flutter"><p>Those three are the whole of it here. <code>MawyFontFamily</code> has no fourth value and there is no <code>fonts</code> list to add one to. The catalogue of families and the stylesheet fetched on first use, which the React half of this section covers, only apply in a browser. A Flutter application declares the fonts it ships in <code>pubspec.yaml</code> long before a viewer is built.</p><p>A bundled face needs a name, which is what <code>fontFamilyName</code> takes:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  defaultTypography</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyTypography</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    fontFamily</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyFontFamily</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.serif,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    fontFamilyName</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Archive&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Left out, each of the three roles maps to whatever the platform uses for that role. Set, it maps to the family named. The role still decides what the toolbar calls it and which of the three is selected, and <code>fontFamilyName</code> decides what is actually drawn.</p><p>The rest of this section applies to the React package only.</p></div><div class="mawy-fw" data-fw="react"><p>Web fonts are one prop away, and they are a prop rather than a default on purpose. A viewer is a component inside somebody else&#39;s page, and one that opens a connection to a font CDN on its own decides for that page about privacy, offline behaviour, and a request its content policy may refuse. So the library ships the list and the application opts in:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MAWY_SYSTEM_FONTS, MAWY_WEB_FONTS, MawyViewer } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;mawy-react&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fonts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MAWY_SYSTEM_FONTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MAWY_WEB_FONTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]} /&gt;;</span></span></code></pre></div><p>Every family in <code>MAWY_WEB_FONTS</code> is under the SIL Open Font License, which permits commercial use, embedding and redistribution. Nothing on the list needs a licence bought for it.</p><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>Sans</strong></td><td>Inter, IBM Plex Sans, <a href="https://www.brailleinstitute.org/freefont/" target="_blank" rel="noreferrer">Atkinson Hyperlegible</a></td></tr><tr><td><strong>Serif</strong></td><td>Source Serif 4, Literata, Lora, EB Garamond</td></tr><tr><td><strong>Mono</strong></td><td>JetBrains Mono</td></tr><tr><td><strong>Korean</strong></td><td>Pretendard, Noto Sans KR, Noto Serif KR, Nanum Myeongjo, Gowun Dodum</td></tr></tbody></table><p>The Korean families are on the list rather than left to the fallback. A typeface menu with only Latin faces leaves a Korean document set in something nobody chose.</p><p>Nothing is fetched until it is needed. The font the document is already set in arrives when the viewer mounts, and the rest arrive when the typeface menu is first opened. They have to arrive then, because every name in that menu is drawn in its own face. A reader who never opens it never requests anything.</p><p>Your own list is the same shape:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  fonts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sans&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;house&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Söhne&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, stack: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&#39;Söhne&#39;, system-ui, sans-serif&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;archive&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Archive&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, stack: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&#39;Archive&#39;, serif&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, href: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/fonts/archive.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ]}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><p><code>id</code> is what <code>typography.fontFamily</code> is set to. <code>stack</code> defaults to <code>var(--mawy-font-{id})</code>, which keeps the three built-in roles in the stylesheet. <code>href</code> is a stylesheet fetched once, the first time the font is drawn. Leave it out for a font the page already loads.</p></div><h2 id="setting-the-type" tabindex="-1">Setting the type <a class="header-anchor" href="#setting-the-type" aria-label="Permalink to &quot;Setting the type&quot;">​</a></h2><div class="mawy-fw" data-fw="react"><p>Every typography value reaches the page as a <code>--mawy-doc-*</code> custom property, so there are two ways to set one and both end up in the same place.</p><p>Through the prop, with the viewer keeping the settings:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  defaultTypography</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{{ fontSize: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, measure: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;wide&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  onTypographyChange</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">typography</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> localStorage.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setItem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;type&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">JSON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">stringify</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(typography))}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><p>Or through CSS, with the toolbar left off entirely:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.reader</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> .mawy-md</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --mawy-doc-size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">18</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">px</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --mawy-doc-line-height</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Anything left out of <code>typography</code> keeps its default, so <code>{ fontSize: 18 }</code> on its own is enough.</p><p>The document&#39;s line height and letter spacing are declared on the text itself, not only on the container around it. An inherited value loses to <em>any</em> declaration on the element, so without this, one <code>article p { line-height: 28px }</code> in the surrounding page would leave the line-height control changing nothing a reader can see.</p><p><strong>An image can be given a box to arrive into.</strong> Markdown has nowhere to write a picture&#39;s width and height, so nothing on the page knows how much room to keep, and everything under it moves when the image loads. The document cannot supply that, but an application that knows the shape of its pictures can:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.my-docs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --mawy-doc-image-aspect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> / </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The box is reserved before the image arrives, and <code>--mawy-doc-image-fit</code> is <code>contain</code>, so a picture of a different shape is letter-boxed rather than cut. Unset is the default, and then no box is reserved. Writing the dimensions in the document itself is the other approach, and it is not a syntax this parser reads today.</p></div><div class="mawy-fw" data-fw="flutter"><p>There is one way to set it, and that is the argument. <code>MawyTypography</code> has a default for every field, so naming one of them is enough and the rest keeps its value:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  defaultTypography</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyTypography</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fontSize</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, measure</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyMeasure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.wide),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  onTypographyChange</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyTypography</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> typography) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> save</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(typography),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><code>defaultTypography</code> sets a starting point and leaves the viewer holding its own settings. <code>typography</code> moves ownership to the application: the toolbar still reports what the reader chose through <code>onTypographyChange</code>, and nothing changes on screen until the application passes the new value back. <code>onTypographyChange</code> is called either way, so the code that remembers a reader&#39;s choice is the same in both.</p><p>To change one field of settings you already have, use <code>copyWith</code>:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _type.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">copyWith</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fontSize</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span></code></pre></div><p>The sizes are logical pixels rather than CSS ones, and the measure widths are 560, 704 and 880. They are the same three columns at the same 16-pixel body size.</p></div><h2 id="the-language-of-the-interface" tabindex="-1">The language of the interface <a class="header-anchor" href="#the-language-of-the-interface" aria-label="Permalink to &quot;The language of the interface&quot;">​</a></h2><p>The toolbar&#39;s labels, the menus, the panel headings and every sentence a screen reader is given are written by the library. <code>locale</code> sets which language they are in:</p><div class="mawy-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">locale</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ko&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div></div><div class="mawy-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document, locale</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyLocale</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.ko)</span></span></code></pre></div></div><p><strong>English and Korean</strong>, and <code>en</code> is the default. It says nothing about the document itself: a Korean document in a viewer whose interface is English is a common case, so the two settings are kept separate.</p><p>Both packages ship the same set of words under the same names, so a screen that reads one way in a browser reads the same way in an app. Adding a language means writing a table in <code>src/internal/i18n.ts</code> and its counterpart in <code>lib/src/internal/i18n.dart</code>. Both are required, and a locale that exists in only one package is not one this library offers.</p><h2 id="theming" tabindex="-1">Theming <a class="header-anchor" href="#theming" aria-label="Permalink to &quot;Theming&quot;">​</a></h2><div class="mawy-fw" data-fw="flutter"><p>The palette is <a href="./../api/#mawytokens"><code>MawyTokens</code></a>, which holds the stylesheet&#39;s custom properties under Dart names, value for value. <code>accent</code> corresponds to <code>--mawy-accent</code>, and both are <code>#5b34ea</code>. The two palettes are <code>MawyTokens.light</code> and <code>MawyTokens.dark</code>, and the viewer picks between them from <code>colorScheme</code> rather than from anything global, so one document can be dark inside a light screen.</p><p>To redeclare one, pass <code>tokens</code>. It takes a function of the brightness rather than a palette, because a viewer settles on its brightness only after it has been handed everything else, from <code>colorScheme</code> or from the platform when that is <code>system</code>. A document that follows the platform needs values ready in both palettes, not only the one it opened on. <code>copyWith</code> builds a palette without rewriting thirty-one colours:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MawyViewer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  value</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tokens</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Brightness</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> brightness) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      MawyTokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(brightness).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">copyWith</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(accent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Color</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0xFFB8005C</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><code>MawyEditor</code> takes the same argument and passes it to its preview, so an editor and the document it is editing are never in two palettes.</p><p>The export is also for an application drawing its own interface beside a document and wanting the same colours in it:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">final</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyTokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tokens </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MawyTokens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Theme</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(context).brightness);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Container</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(color</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tokens.backgroundSunken, child</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* … */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><code>MawyRadius</code> and <code>MawyMotion</code> work the same way for the corner radii and for the single duration and curve that everything animated shares.</p></div><div class="mawy-fw" data-fw="react"><p>Every colour the viewer draws with is a <code>--mawy-*</code> custom property declared on <code>.mawy-root</code>. Theming means redeclaring one:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.mawy-root</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --mawy-accent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">#b8005c</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  --mawy-radius-lg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">px</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>They are on <code>.mawy-root</code> rather than on <code>:root</code> on purpose. A component library has no reason to write to the document element, and a viewer that read its palette from <code>:root</code> could not be dark inside a light page, which a single embedded document often needs to be.</p></div><p>The light and dark palettes are chosen by <code>colorScheme</code>, which is <code>system</code> unless you set it. <code>system</code> follows the platform setting: <code>prefers-color-scheme</code> in a browser, the platform brightness in an app. <code>light</code> and <code>dark</code> do not follow it, so an application with its own switch can drive the viewer from that, and a reader on a dark machine still gets the light document you asked for.</p><h2 id="mapping-the-page-back-to-the-source" tabindex="-1">Mapping the page back to the source <a class="header-anchor" href="#mapping-the-page-back-to-the-source" aria-label="Permalink to &quot;Mapping the page back to the source&quot;">​</a></h2><div class="mawy-fw" data-fw="flutter"><p>The ranges are in the Dart tree too, with the same offsets: every <code>MdNode</code> carries one, text nodes included. What is missing is an element to hang them on, since there is no DOM, so an application reads a range from <a href="./../api/#parsemarkdown"><code>parseMarkdown</code></a> rather than from the screen. The rest of this section, which covers the attribute, applies to the React package only.</p></div><div class="mawy-fw" data-fw="react"><p>Every element the viewer draws carries <code>data-mawy-range=&quot;start,end&quot;</code>. Those are the offsets, in the Markdown it was given, of that piece&#39;s first character and of the one after its last. Blocks, list items, table rows and cells all carry one, as do the inline elements inside them: emphasis, links, code spans and images.</p><p>A code block carries two ranges. The box around it covers the whole thing, fences and info string and indent. The <code>code</code> element inside covers the code alone, which is the part a caret can be in. That position exists even with nothing between the fences, where the two offsets are the same number.</p><p>In a document that reads <code># Title</code>, a blank line, <code>## Second</code>, that second heading is drawn as:</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">h2</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;second&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> class</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mawy-md-heading&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> data-mawy-range</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;9,18&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Second&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">h2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>A range is the only way back from a place on the page to the place in the document it was drawn from. The editor&#39;s <code>split</code> reads it in two places: to scroll the preview to the block the top line of the source is in, and to put the caret on the word a click in the preview landed on. An application can use it the same way, for a comment pinned to a paragraph or an &quot;edit this section&quot; control beside a heading. The offsets index the string you passed, in UTF-16 code units, so <code>value.slice(start, end)</code> is the Markdown behind whatever was clicked.</p><p>Text nodes are the one thing on the page with no range, having no attributes to put one in. They do not need one. A run of text is bounded by the elements on either side, which is enough to locate it in the source between them: a <code>&lt;strong&gt;</code> drawn from <code>**bold**</code> contains <code>bold</code> at exactly one place inside those eight characters.</p></div><h2 id="accessibility" tabindex="-1">Accessibility <a class="header-anchor" href="#accessibility" aria-label="Permalink to &quot;Accessibility&quot;">​</a></h2><div class="mawy-fw" data-fw="react"><ul><li>The toolbar is a <code>toolbar</code> with one tab stop and arrow-key movement inside it.</li><li>Every icon button has a name; nothing is announced as &quot;button&quot;.</li><li>Menus close on <code>Escape</code> and give the focus back to the control that opened them.</li><li>Following an outline entry moves the focus as well as the scroll, so the next <code>Tab</code> carries on from the heading rather than from the panel.</li><li>A code block&#39;s copy button is invisible until the pointer or the focus is on it, and is never removed from the layout. <code>Tab</code> walks past a button that is not in the layout.</li><li>A wide table and a long line of code each scroll sideways inside their own box, and each is a tab stop, so a keyboard reaches the right-hand end of them.</li><li><code>Escape</code> puts the tooltips away, and the next move of the pointer brings them back. Nothing is lost, because every button they name already says the same words to a screen reader.</li><li>The interface declares the language of its own words, and says nothing about the document&#39;s. That language is the author&#39;s, and this library does not know it.</li><li>Animation is dropped under <code>prefers-reduced-motion</code>.</li></ul><p><strong>The two headings this library writes are <code>h2</code>, and they stay that way.</strong> They are the outline panel&#39;s title and the empty state&#39;s. The document&#39;s own heading levels belong to the author, so <code># </code> is an <code>h1</code> wherever the viewer is put. Inside a page whose outline makes <code>h2</code> wrong, restyle or relabel them from the outside: they are <code>.mawy-outline-title</code> and <code>.mawy-empty-title</code>. Even a prop for the level would leave the document&#39;s own levels where the author put them.</p></div><div class="mawy-fw" data-fw="react"><p>Every surface is run through <a href="https://github.com/dequelabs/axe-core" target="_blank" rel="noreferrer">axe</a> on every change: the viewer with a document and as the file picker, all four editor modes, the outline open, a menu open, and find and replace open, each in both palettes. What that found is already in the list above. A task list&#39;s checkbox is named by the line beside it, the palette&#39;s faintest text was raised until it meets AA on both backgrounds, the editing surface is a <code>div</code> rather than an <code>article</code> because ARIA does not allow a document section to be a <code>textbox</code>, and the link back from a footnote is underlined rather than only coloured.</p><p>An automated pass is a minimum. What it cannot catch, such as arrow keys inside the toolbar or where the focus lands after an outline entry, is asserted one case at a time in the same suite.</p></div><div class="mawy-fw" data-fw="flutter"><ul><li>Every control is a <code>Semantics</code> button with a name and, where it toggles, a state, so a screen reader reads it as a toolbar.</li><li>The toolbar is one tab stop, and the arrows move between the controls inside it. <code>Home</code> and <code>End</code> go to the ends of the row, and <code>Enter</code> and the space bar press whichever control the focus is on.</li><li>A menu opens with the focus already in it, closes on <code>Escape</code>, and gives the focus back to the button it came from. It has to open that way here, unlike in the React package: the panel is put up through the <code>Overlay</code>, so it is nowhere near its own button in the order <code>Tab</code> walks, and reaching it would mean travelling the whole document.</li><li>The outline&#39;s entries are tab stops of their own, one press of <code>Tab</code> each, the way the React package&#39;s <code>&lt;button&gt;</code>s in an <code>&lt;ol&gt;</code> are. They do not use the toolbar&#39;s single stop with arrow keys, because a short list of headings is not worth a second way of moving through it.</li><li>Headings, links and images carry their own semantics through the document, and following an outline entry moves the focus as well as the scroll, so the next <code>Tab</code> carries on from the heading rather than from the panel. The heading itself is not a tab stop. It uses <code>skipTraversal</code>, the Dart equivalent of the web&#39;s <code>tabIndex = -1</code>, so the focus can be put there but <code>Tab</code> does not stop on it.</li><li>Text scales with the platform&#39;s own text size, because the sizes are logical pixels through <code>MawyTypography</code> rather than anything baked in.</li><li>Animation is dropped where the platform&#39;s reduce-motion setting asks for it. It reads <code>MediaQuery.disableAnimationsOf</code>, which is the same setting the stylesheet reads as <code>prefers-reduced-motion</code>, so a reader who turned it on gets the same result from both packages.</li></ul></div><h2 id="printing" tabindex="-1">Printing <a class="header-anchor" href="#printing" aria-label="Permalink to &quot;Printing&quot;">​</a></h2><p>On paper, only the document is printed. The toolbar, the outline, the status bar and the find box are all controls to press, which paper cannot do, so none of them prints. Neither does a footnote&#39;s link back to where it was mentioned.</p><div class="mawy-fw" data-fw="react"><p>Three other things change for paper. A viewer given a height scrolls inside it, and a box with a height prints one screenful and loses the rest, so on paper it grows as tall as the document. The palette goes dark-on-light whatever the reader chose, because printing a dark theme fills the page with ink. And a link&#39;s destination is written out after it, since &quot;see the docs&quot; on paper points nowhere.</p><p>In the editor, the drawn document is what prints. In <code>split</code> the source pane is left out. In <code>plain</code>, where there is no drawn document, the source prints as text rather than as a <code>&lt;textarea&gt;</code>, because a textarea puts only what is inside its own box on the page and loses the rest of the file.</p><p>Nothing has to be switched on. It is <code>@media print</code> in the stylesheet already imported, so <code>Ctrl</code>+<code>P</code> on a page with a viewer on it works as it should.</p></div><div class="mawy-fw" data-fw="flutter"><p>Printing is handled by the platform rather than the package. An app prints through a plugin or the operating system&#39;s own sheet, and what it hands over is a widget tree rather than a page. This section applies to the React package only.</p></div>`,35))])}const b=h(d,[["render",c]]);export{m as __pageData,b as default};
