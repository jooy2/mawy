# The gallery is held to the package's own rules, minus the one about
# documenting a public API: nothing here is a public API.
include: package:flutter_lints/flutter.yaml

formatter:
  page_width: 100

linter:
  rules:
    - always_declare_return_types
    - prefer_single_quotes
    - directives_ordering
    - use_super_parameters
