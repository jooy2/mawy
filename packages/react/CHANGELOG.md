# Changelog

> This package's history. Each language Mawy ships for keeps its own changelog beside its own manifest, because they version independently.

## Unreleased

Nothing released yet. The repository scaffolding is in place — packaging, linting, tests in a real browser, CI and the documentation site — and the editor is being built on top of it.
